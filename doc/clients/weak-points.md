# TDS Geo — Client Weak Points & Remediation Plan

| Client | Type | Critical | High | Medium | Low |
|---|---|---|---|---|---|
| Boston Pharma | WordPress | 2 | 4 | 3 | 2 |
| Boston Vet | WordPress | 2 | 3 | 1 | 1 |
| Traffic Test | Shopify | 1 | 3 | 2 | 1 |
| Infrastructure | System | 2 | 3 | 2 | 1 |

---

## 1. Boston Pharmaceutical Industries

### 🔴 Critical (Fix Immediately)

| # | Issue | Detail | Fix |
|---|---|---|---|
| WP-1 | **76 products have $0 price** | Every single WooCommerce product has price=0 or empty. Customers can view products but cannot purchase anything. The entire e-commerce function of the site is broken. | Add prices via WooCommerce → Products → Edit each product → Set regular price. If products are informational (not for sale), switch from "Simple product" to "External/Affiliate" or hide from catalog. |
| WP-2 | **0 products have descriptions** | All 76 products lack product descriptions. Google Shopping feeds, structured data, and conversion all suffer. | Add descriptions (min 100 words) to each product. Use the writer role for consistent brand voice. |

### 🟠 High Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| WP-3 | **TTFB 1.1 seconds** | Server response time on Bluehost shared hosting is slow. Google Core Web Vitals penalizes this. Hurts SEO ranking for product and category pages. | Upgrade hosting plan, enable better caching, or migrate to faster host. At minimum: enable nginx FastCGI cache. |
| WP-4 | **HSTS max-age only 300 seconds** | Security header instructs browsers to only enforce HTTPS for 5 minutes. Should be minimum 6 months (15768000). Security rating is downgraded. | Add to .htaccess: `Header always set Strict-Transport-Security "max-age=15768000; includeSubDomains"` |
| WP-5 | **0 featured products** | No products are marked as featured. The WooCommerce featured products widget/section is empty, wasting prime homepage real estate. | Select 8-12 flagship products, mark as featured in product edit screen. |
| WP-6 | **20 draft articles unpublished** | Articles were generated by TDS Geo pipeline but never published. Content investment is sitting idle. | Review and publish via TDS Geo backend or directly in WordPress. |

### 🟡 Medium Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| WP-7 | **No product categorization used on frontend** | Category distribution shows products not properly organized into WooCommerce categories for browsing. | Assign each product to the correct category (Dietary Supplements, Multi-Vitamins, Immunity Boosters, Pregnancy, etc.) |
| WP-8 | **Shared hosting limits scalability** | Endurance/Bluehost shared plan has resource limits. Under traffic spikes, site will slow or crash. | Monitor traffic. If exceeds 500 visitors/day, upgrade to VPS or dedicated. |
| WP-9 | **Weglot auto-translate may produce low-quality Arabic** | Automatic translation (not human-reviewed) can produce awkward Arabic copy that damages brand credibility. | Review top 20 product pages in Arabic. Manually correct translations. |

### 🔵 Low Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| WP-10 | **No SSL preload** | HSTS preload not configured. Browsers first visit could still use HTTP. | Add `preload` directive to HSTS and submit to hstspreload.org. |
| WP-11 | **Only 1 blog category** | All 84 posts are in "Blog" category. No topical organization for SEO siloing. | Create sub-categories (Product Education, Industry Insights, Wellness, Regulatory) and recategorize posts. |

---

## 2. Boston Veterinary Pharmaceutical

### 🔴 Critical (Fix Immediately)

| # | Issue | Detail | Fix |
|---|---|---|---|
| BV-1 | **FULLY BLOCKED by Cloudflare** | Our production server IP (16.192.29.174) returns 403 on every endpoint — homepage, API, wp-admin, TDS Geo plugin, sitemap. Cannot publish articles, maintain site, or audit content until resolved. | Add IP 16.192.29.174 to Cloudflare WAF Allow list under Security → WAF → Tools → IP Access Rules → Allow. |
| BV-2 | **Cannot verify site health** | Since BV-1, we cannot run any diagnostics. Unknown: plugin status, WP version, PHP version, active plugins, security patches. | After IP whitelisting, run full audit. |

### 🟠 High Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| BV-3 | **Likely same $0 pricing issue** | Built by same team as Boston Pharma on same WooCommerce setup. Sister site likely has same product pricing issue. | Log into wp-admin → WooCommerce → Products → Check prices. If $0, fix per WP-1. |
| BV-4 | **No contact info on brand profile** | No phone, email, or direct contact discovered for the site owner (different from shared Boston Group info). | Check wp-admin → Users for admin email. Check site footer/contact page for correct details. |
| BV-5 | **TDS Geo plugin status unknown** | Plugin may not be active or installed. No way to verify until Cloudflare is resolved. | After IP whitelisted, check Plugins → TDS Geo is active. Re-install if needed from /wp-admin/plugin-install.php |

### 🟡 Medium Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| BV-6 | **No content published yet** | Unlike Boston Pharma (20+ articles), Boston Vet likely has minimal to no published content. Entire content strategy is pending. | Generate and publish 12-24 articles per month using the writer role / content plan. |

### 🔵 Low Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| BV-7 | **No brand differentiation from Boston Pharma** | Same parent company (Boston Group), same visual identity likely. Site needs to establish distinct veterinary identity. | Update logo/colors to use green/animal-health palette. Content should be 100% veterinary-focused. |

---

## 3. Traffic Test (Shopify)

### 🔴 Critical (Fix Immediately)

| # | Issue | Detail | Fix |
|---|---|---|---|
| ST-1 | **Expiring OAuth token with no refresh** | Shopify token expires in ~1 hour. No auto-refresh logic implemented. Once expired, the backend cannot publish articles or manage the store. | Implement token refresh in `backend/routes/shopifyInstall.ts` using the `refresh_token`. Or schedule a cron to re-authorize. |

### 🟠 High Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| ST-2 | **No custom domain** | Store runs on traffic-test.myshopify.com. Cannot rank in Google. Looks unprofessional. Not a real production storefront. | Add custom domain in Shopify Settings → Domains. Point DNS to Shopify. |
| ST-3 | **17 test products only** | Limited product catalog. Most products are test/lorem items. No real e-commerce operation exists. | Populate with actual products or repurpose as a real store. |
| ST-4 | **Basic App Development plan** | Lowest Shopify tier. Missing features: gift cards, professional reports, advanced analytics, custom checkout. | Upgrade to Basic ($39/mo) or Shopify ($105/mo) for production use. |

### 🟡 Medium Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| ST-5 | **Mixed theme management** | 3 themes installed (Horizon, debut-vintage, test-data). Only "test-data" published. Bloated theme library. | Delete unused themes. Test and publish a proper production theme. |
| ST-6 | **US Eastern timezone, USD, lbs** | Settings are US-based, not relevant to Egyptian market. All content and pricing will reflect this mismatch. | Update to Africa/Cairo timezone, EGP currency, kg weight unit if targeting Egypt. |

### 🔵 Low Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| ST-7 | **No analytics configured** | GA4 or Shopify Analytics not set up. Cannot measure traffic, conversion, or content performance. | Install GA4 via Shopify → Settings → Customer events. Set up conversion tracking. |

---

## 4. Infrastructure & System

### 🔴 Critical (Fix Immediately)

| # | Issue | Detail | Fix |
|---|---|---|---|
| SYS-1 | **Disk at 67% — 26G/38G used** | Growing at ~2-3% per week. At current rate, disk full in 2-3 months. Docker images, logs, and DB bloat are main consumers. | `docker system prune -af` (clean unused images), rotate logs (`logrotate`), `vacuumdb` on Postgres. Add 20GB EBS volume. |
| SYS-2 | **Backend restarted 4 times** | PM2 logged 4 restarts on the backend, likely from the `ReferenceError: n is not defined` bug. Fix was deployed but root cause may still lurk under edge cases. | Monitor PM2 logs for recurring crashes. Set `max_restarts=10` in ecosystem.config to prevent restart loops. |

### 🟠 High Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| SYS-3 | **3 unreachable services** | TuRoVec (8530), AirLLM (8531), OpenSEO (3005) all down. Vector search, local LLM, and SEO analysis are non-functional. Content quality may degrade without these. | Restart Docker containers: `docker compose up -d turbovec airllm openseo`. Check logs for errors. |
| SYS-4 | **Redis not configured** | No job queue — content generation falls back to synchronous execution. Under load, this blocks the API and degrades performance. | Set `REDIS_URL` in .env. Start Redis on port 6379 or use ElastiCache. |
| SYS-5 | **No IAM role / S3 backup** | DB backup exists locally but not off-site. If EC2 instance fails, all data is lost except DB (which has local pg_dump only). | Create IAM role with S3 write access. Add `aws s3 cp` to backup-db.sh. |

### 🟡 Medium Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| SYS-6 | **No alerting/monitoring** | No uptime monitoring, no error alerts. Outages go unnoticed until manual PM2 check. | Set up UptimeRobot (free) for HTTP monitoring. Add Slack/Discord webhook for PM2 crash events. |
| SYS-7 | **Ollama cloud single provider** | Only one AI provider configured. If Ollama goes down, content generation stops entirely. | Add OpenAI key as fallback. In `config.ts`, set `OPENAI_API_KEY` and model fallback chain. |

### 🔵 Low Priority

| # | Issue | Detail | Fix |
|---|---|---|---|
| SYS-8 | **No staging environment** | All development/testing runs on production server. Risk of breaking live site during updates. | Spin up a t3a.micro staging instance. Deploy changes there first, then promote to production. |
| SYS-9 | **No CI/CD pipeline** | Deployments are manual via scp/ssh. No version control integration, no rollback capability. | Set up GitHub Actions to deploy on git push. Add `ecosystem.config.cjs` with proper deploy scripts. |

---

## Remediation Priority Matrix

| Fix | Effort | Impact | Client | Suggested Timeline |
|---|---|---|---|---|
| Whitelist IP in Cloudflare (BV-1) | 5 min | ✅ Unlocks entire Boston Vet | Boston Vet | Before next content cycle |
| Add product prices (WP-1) | 2-4 hrs | ✅ Enables WooCommerce sales | Boston Pharma | This week |
| Clean disk space (SYS-1) | 30 min | ✅ Prevents crash | System | This week |
| Add product descriptions (WP-2) | 3-5 hrs | ✅ SEO + conversion boost | Boston Pharma | This week |
| Implement Shopify token refresh (ST-1) | 2 hrs | ✅ Maintains publishing ability | Traffic Test | This week |
| Fix HSTS (WP-4) | 5 min | ✅ Security improvement | Boston Pharma | This week |
| Restart Docker services (SYS-3) | 10 min | ✅ Restores 3 features | System | This week |
| Publish 20 drafts (WP-6) | 1 hr | ✅ Content ROI | Boston Pharma | This week |
| Redis setup (SYS-4) | 1 hr | ⚡ Performance | System | Next week |
| IAM + S3 backup (SYS-5) | 1 hr | ✅ Disaster recovery | System | Next week |

---

*Generated by TDS Geo — July 4, 2026*
*Next review: August 4, 2026*
